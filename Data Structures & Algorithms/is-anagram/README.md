## Approach
- **Solution 1**: We utilize two hash tables to record the number of times each character appears. Then we compare two hash tables.

---
## Complexity
|Solution|time|space|
|---|:---|:---|
|**Solution 1**|amortized expected `O(m+n)`|worst case `O(m+n)`|
